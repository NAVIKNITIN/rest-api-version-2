console.log(window);
// you can see all the properties which are available on windows and pre defined in the system.

// EXPRESS API
let app = express();
app.listen(4800 , () => console.log("hello world "));

// CRUD Operatiions "create " , "recieve"  , update ,delete
